package com.pantawin.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette.
val PantawinBlue = Color(0xFF1565C0)
val PantawinBlueDark = Color(0xFF0D47A1)
val PantawinAccent = Color(0xFF00ACC1)

// Semantic status tokens (spec section 6.6). Status is always icon + color +
// text — these colors are chosen to stay distinguishable for common
// colorblindness and to meet WCAG contrast on both light and dark surfaces.
val StatusUp = Color(0xFF2E7D32)
val StatusDown = Color(0xFFC62828)
val StatusPaused = Color(0xFFF9A825)
val StatusPending = Color(0xFF757575)
val StatusDegraded = Color(0xFFEF6C00)
