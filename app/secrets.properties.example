# Copy to app/secrets.properties (gitignored) and fill in the real values —
# matches the ADMIN_EMAIL/ADMIN_PASSWORD you set in deploy/.env on the VPS.
#
# Temporary, M0-only: the dashboard screen logs in with these on launch
# since there's no login screen yet (that lands in M1 alongside full
# monitor CRUD and JWT middleware on every route).
ADMIN_EMAIL=
ADMIN_PASSWORD=
API_BASE_URL=https://api.pantawin.gratisaja.com
